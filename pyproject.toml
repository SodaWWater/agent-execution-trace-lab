[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-trace-lab"
version = "0.1.0"
description = "Deterministic local agent execution trace experiment"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
